name: build-windows-release

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t my-qt6-win -f Dockerfile .

      - name: Prepare writable home for Wine
        run: mkdir -p "$GITHUB_WORKSPACE/.ci-home"

      - name: Build Windows app
        run: |
          docker run --rm \
            --user "$(id -u):$(id -g)" \
            -e HOME=/tmp/home \
            -e WINEPREFIX=/tmp/home/.wine \
            -v "$GITHUB_WORKSPACE:/home/user/project" \
            -v "$GITHUB_WORKSPACE/.ci-home:/tmp/home" \
            my-qt6-win

      - name: Create release metadata
        id: meta
        run: |
          TAG="pre-${GITHUB_RUN_NUMBER}-${GITHUB_SHA::7}"
          NAME="Windows prerelease ${GITHUB_RUN_NUMBER}"
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "name=$NAME" >> "$GITHUB_OUTPUT"

      - name: Publish prerelease
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.meta.outputs.tag }}
          target_commitish: ${{ github.sha }}
          name: ${{ steps.meta.outputs.name }}
          prerelease: true
          generate_release_notes: true
          files: |
            build-win/app-win64.zip